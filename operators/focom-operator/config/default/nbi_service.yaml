apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: focom-operator
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-nbi-service
  namespace: system
spec:
  ports:
  - name: nbi-http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    control-plane: controller-manager
  type: ClusterIP