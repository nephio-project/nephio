apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - focom-resources-gitrepository.yaml
  - focom-resources-kustomization.yaml
  # Uncomment to enable webhook support (instant sync)
  # Run ./setup-webhook.sh first to create the webhook-token secret
  # - focom-resources-receiver.yaml
